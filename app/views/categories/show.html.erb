<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <div style="display: flex">
    <%= button_to "Edit this category", edit_category_path(@category) %> 
    <%= button_to "Destroy this category", @category, method: :delete, data: { confirm: 'Category including its tasks will be erased. Are you sure you want to delete this??' } %>
  </div>
  <%= link_to "Back to categories", categories_path %>
  <div>------------------------------------------------------------</div>


  <div>


  <% @category.tasks.each do |task| %>  
  <p>
    <%= link_to task.name, category_task_path(@category.id, task) %>
    <%=link_to "Edit", edit_category_task_path(@category.id, task),
                       style: "display: inline"%>
    <%= link_to 'Delete', [task.category, task],
                          method: :delete,
                          data: { confirm: 'Are you sure?' } %>
  </p>

      <% end %>
  <%= link_to 'New Task', new_category_task_path(@category) %>
  </ul>
 <h3> <%=link_to "Tasks for today", tasks_today_path(@category)%></h3>
   
</div>
